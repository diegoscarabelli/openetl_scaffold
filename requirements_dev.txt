# Main requirements
-r requirements.txt

# Orchestrators (both installed for testing).
apache-airflow>=3.0,<4.0 ; python_version >= "3.10" and python_version < "3.13"
prefect>=3.0,<4.0

# Date/time utilities.
pendulum>=3.0

# Linting and formatting
autoflake
black>=26.3.1
docformatter
sqlfluff

# Testing
pytest
pytest-cov
pytest-env
python-dotenv

pre-commit
